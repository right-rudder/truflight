---
const { data } = Astro.props;
import ImageComp from "./ImageComp.astro";
---

<div class="relative mt-18 flex h-[60vh] overflow-hidden object-cover lg:mt-24">
  <ImageComp
    src={data.primaryImage.src || data.primaryImage}
    alt={data.primaryImage.alt || data.primaryAlt}
    classes={`object-cover h-full w-full`}
    loading="eager"
  />

  <div
    class="absolute inset-0 flex items-center justify-center px-5 text-center text-white backdrop-brightness-50 md:px-10 lg:px-20"
  >
    <div class="flex translate-y-5 flex-col items-center gap-3">
      <h1
        class="font-display max-w-2xl text-4xl font-black italic lg:max-w-6xl lg:text-6xl xl:text-7xl"
      >
        {data.title}
      </h1>
      {
        data.subtitle && (
          <h2 class="max-w-3xl text-xl font-semibold text-pretty lg:text-2xl xl:text-3xl">
            {data.subtitle}
          </h2>
        )
      }
      {
        data.button && (
          <a href={data.button.href} class="btn-primary shadow-none!">
            {data.button.text}
          </a>
        )
      }
    </div>
  </div>
</div>
